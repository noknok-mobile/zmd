extends /layout/template

block content
    .page__grid
        .page__sidebar
            +sidebar-menu(menu)
        .page__content
            .page-section
                .branch-list.branch-list_table
                    +branch({text:'Профиль', icon: 'icons.svg#tomo'}).tile.branch_tile
                    +branch({text:'Мои записи', icon: 'icons.svg#tomo'}).tile.branch_tile

block prepend vars 
    - const title = "Личный кабинет"
block append vars
    - menu = ['профиль', 'мои записи']